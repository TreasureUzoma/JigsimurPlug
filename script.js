function catalogue(){
  var targetDiv = document.getElementById("catalogue");
  // Scroll to the targetDiv's top position
  window.scrollTo({
    top: targetDiv.offsetTop,
    behavior: "smooth" // Smooth scrolling
  });
}
function about(){
  var targetDiv = document.getElementById("about");
  // Scroll to the targetDiv's top position
  window.scrollTo({
    top: targetDiv.offsetTop,
    behavior: "smooth" // Smooth scrolling
  });
}
/*function menu(){
  var targetDiv = document.getElementById("footer");
  // Scroll to the targetDiv's top position
  window.scrollTo({
    top: targetDiv.offsetTop,
    behavior: "smooth" // Smooth scrolling
  });
}
function menu(){
  var targetDiv = document.getElementById("footer");
  // Scroll to the targetDiv's top position
  window.scrollTo({
    top: targetDiv.offsetTop,
    behavior: "smooth" // Smooth scrolling
  });
}
function menu(){
  var targetDiv = document.getElementById("footer");
  // Scroll to the targetDiv's top position
  window.scrollTo({
    top: targetDiv.offsetTop,
    behavior: "smooth" // Smooth scrolling
  });
}*/